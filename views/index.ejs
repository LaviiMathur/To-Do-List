<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Capstone</title>
    <link rel="stylesheet" href="/styles/style.css" />
    <link rel="preconnect" href="https://fonts.googleapis.com" />
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />

    <link
      href="https://fonts.googleapis.com/css2?family=Poppins:wght@100;300;500;900&display=swap"
      rel="stylesheet"
    />
    <link
      rel="stylesheet"
      href="https://fonts.googleapis.com/css2?family=Material+Symbols+Outlined:opsz,wght,FILL,GRAD@20..48,100..700,0..1,-50..200"
    />
  </head>
  <body>
    <header>
      <nav>
        <ul>
          <li>
            <a href="/"
              >Today <span class="material-symbols-outlined"> today </span></a
            >
          </li>
          <li>
            <a href="/work"
              >Work <span class="material-symbols-outlined"> work </span></a
            >
          </li>
        </ul>
      </nav>
    </header>
    <main>
      <div class="containor">
        <h2 id="heading" class="box">
          <%= new Intl.DateTimeFormat("en-GB", { dateStyle: "full" }).format(new Date()) %>
        </h2>
        <form action="/reset" method="post">
        <button  id="reset">Clr All</button></form>
        <div class="box">
         
          <% if (locals.listItems) {%>
          <%for (let i = 0; i < locals.listItems.length ; i++) {%>
             <div class="item">
            <input class = "checkbox"type="checkbox" />
            <p class="list"><%= listItems[i] %></p>
          </div>
            
          <% }}  %>
        
          <form action="/submit" class="item" method="POST">
            <input type="text"  placeholder="New item" autocomplete="off" name="newItem" autocapitalize="on"/>
            <button type="submit" id="submit">+</button>
          </form>
        </div>
      </div>
    </main>
    <footer><p>copyright &#169; <%= new Date().getFullYear() %></p></footer>
  </body>
</html>
